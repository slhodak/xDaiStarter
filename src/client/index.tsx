import React from 'react';
import ReactDOM from 'react-dom';
import XDaiStarter from './components/XDaiStarter';


ReactDOM.render(
  <React.StrictMode>
    <XDaiStarter />
  </React.StrictMode>,
  document.getElementById('root')
);
